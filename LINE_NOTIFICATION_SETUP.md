# LINE Notification Setup Guide

คู่มือการตั้งค่าระบบแจ้งเตือนผ่าน LINE เมื่อมีผู้สมัคร Seller ใหม่

## ภาพรวม

ระบบนี้จะส่งการแจ้งเตือนไปยัง LINE ของ Admin อัตโนมัติเมื่อ:
- มีผู้สมัคร Seller ใหม่ผ่าน Email/Password
- มีผู้สมัคร Seller ใหม่ผ่าน Google OAuth
- Seller กรอกข้อมูลโปรไฟล์เสร็จสมบูรณ์ (เพิ่มเติมในอนาคต)

## ขั้นตอนการตั้งค่า

### 1. สร้าง LINE Official Account

1. ไปที่ [LINE Official Account Manager](https://manager.line.biz/)
2. เข้าสู่ระบบด้วยบัญชี LINE ของคุณ
3. คลิก **"Create"** หรือ **"สร้าง"** เพื่อสร้าง LINE Official Account ใหม่
4. กรอกข้อมูลบัญชี:
   - **Account name**: ชื่อบัญชี (เช่น "Paydee Admin Notifications")
   - **Category**: เลือกหมวดหมู่ที่เหมาะสม (เช่น IT/Technology)
   - **Description**: คำอธิบาย (ไม่บังคับ)

### 2. เปิดใช้งาน Messaging API

1. จาก LINE Official Account Manager ไปที่บัญชีที่สร้างไว้
2. คลิกเมนู **Settings** (ตั้งค่า)
3. ไปที่แท็บ **Messaging API**
4. คลิก **"Enable Messaging API"** หรือ **"เปิดใช้งาน Messaging API"**
5. เลือกหรือสร้าง Provider ใหม่
6. กรอกข้อมูลที่จำเป็น

### 3. รับ Channel Access Token

1. หลังจากเปิดใช้งาน Messaging API แล้ว ให้ scroll ลงมาที่ส่วน **"Channel access token"**
2. คลิก **"Issue"** หรือ **"ออก"** เพื่อสร้าง token ใหม่
3. **คัดลอก token ที่ได้** (จะเป็นข้อความยาวๆ)
4. **เก็บ token นี้ไว้เป็นความลับ** อย่า commit เข้า Git หรือแชร์ให้ใครเห็น

### 4. ตั้งค่า Webhook (ไม่บังคับ แต่แนะนำ)

สำหรับการรับ User ID และ Group ID

1. ในหน้า Messaging API Settings ไปที่ส่วน **"Webhook settings"**
2. ตั้งค่า Webhook URL (ถ้ามี) หรือข้ามขั้นตอนนี้ไปก่อน
3. เปิดใช้งาน **"Use webhook"**

### 5. หา User ID หรือ Group ID

มี 2 วิธีในการรับการแจ้งเตือน:

#### วิธีที่ 1: ส่งไปยัง User เดี่ยว (แนะนำสำหรับ Admin คนเดียว)

**ใช้เครื่องมือ LINE Bot Designer หรือ LINE Developers Console:**

1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. เลือก Provider และ Channel ที่สร้างไว้
3. ไปที่แท็บ **"Messaging API"**
4. หา **"Your user ID"** ในส่วนด้านล่าง
5. คัดลอก User ID

**หรือใช้วิธีดึง User ID จาก Profile API:**

```bash
# เพิ่ม LINE Official Account เป็นเพื่อนใน LINE app ของคุณ
# จากนั้นใช้ curl command นี้ (แทน YOUR_CHANNEL_ACCESS_TOKEN):

curl -X GET \
  'https://api.line.me/v2/bot/profile/{userId}' \
  -H 'Authorization: Bearer YOUR_CHANNEL_ACCESS_TOKEN'
```

**วิธีง่ายที่สุด:**
1. เพิ่ม LINE Official Account เป็นเพื่อนใน LINE app ของคุณ
2. ส่งข้อความไปหา Official Account นั้น
3. ดูใน Webhook events (ถ้าได้ตั้งค่า webhook ไว้) จะเห็น User ID ของคุณ

#### วิธีที่ 2: ส่งไปยัง Group Chat (แนะนำสำหรับทีม Admin หลายคน)

1. สร้าง LINE Group Chat
2. เชิญสมาชิกทีม Admin ทุกคนเข้ากลุ่ม
3. เชิญ LINE Official Account เข้ากลุ่ม:
   - เปิด Group Chat
   - คลิก **"เชิญ"** หรือไอคอน + ที่มุมบนขวา
   - ค้นหาและเลือก LINE Official Account ของคุณ
4. หา Group ID:
   - ส่งข้อความอะไรก็ได้ในกลุ่ม
   - ดูใน Webhook events จะเห็น Group ID (เริ่มต้นด้วย `C`)

**ตัวอย่าง Webhook Event:**
```json
{
  "events": [
    {
      "type": "message",
      "source": {
        "type": "group",
        "groupId": "Cxxxxxxxxxxxxxxxxxxxxxxxxxxxx"  // นี่คือ Group ID
      }
    }
  ]
}
```

### 6. ตั้งค่า Environment Variables

เพิ่ม environment variables ต่อไปนี้ใน `.env.local`:

```bash
# LINE Messaging API Configuration
LINE_CHANNEL_ACCESS_TOKEN=your_channel_access_token_here

# เลือก 1 ใน 2: User ID (สำหรับแอดมินคนเดียว) หรือ Group ID (สำหรับทีม)
LINE_ADMIN_USER_ID=U1234567890abcdefghijklmnopqrstu  # สำหรับส่งแจ้งเตือนถึง Admin คนเดียว
# หรือ
LINE_ADMIN_GROUP_ID=Cxxxxxxxxxxxxxxxxxxxxxxxxxxxx     # สำหรับส่งแจ้งเตือนถึงกลุ่ม Admin
```

**หมายเหตุ:**
- ใส่เพียงอย่างใดอย่างหนึ่ง: `LINE_ADMIN_USER_ID` หรือ `LINE_ADMIN_GROUP_ID`
- ถ้าใส่ทั้งสอง ระบบจะใช้ `LINE_ADMIN_USER_ID` เป็นหลัก
- **อย่า commit ไฟล์ `.env.local` เข้า Git!**

### 7. ทดสอบการแจ้งเตือน

1. รีสตาร์ท development server:
   ```bash
   npm run dev
   ```

2. ทดสอบสมัครสมาชิกใหม่:
   - ไปที่หน้า Register: `http://localhost:3000/auth/register`
   - สมัครด้วย email ใหม่ หรือ Google account
   - ตรวจสอบว่า LINE notification ถูกส่งไปยัง User/Group ที่ตั้งไว้

3. ตรวจสอบ logs:
   ```bash
   # ดู console logs เพื่อยืนยันว่าการส่ง notification สำเร็จ
   # จะเห็นข้อความ: "LINE notification sent successfully for new seller registration: ..."
   ```

## รูปแบบข้อความแจ้งเตือน

เมื่อมีผู้สมัคร Seller ใหม่ ข้อความจะมีรูปแบบดังนี้:

```
🎉 มีผู้สมัคร Seller ใหม่รอการอนุมัติ!

📧 อีเมล: user@example.com
👤 ชื่อ: John Doe
🔐 วิธีสมัคร: Email/Password
📅 เวลา: 7 พฤศจิกายน 2568 14:30

กรุณาเข้าสู่ระบบเพื่ออนุมัติผู้สมัครท่านนี้
```

## การแก้ไขปัญหา

### ไม่ได้รับ notification

1. **ตรวจสอบ Environment Variables:**
   ```bash
   # ตรวจสอบว่าตั้งค่าครบถ้วน
   echo $LINE_CHANNEL_ACCESS_TOKEN
   echo $LINE_ADMIN_USER_ID  # หรือ LINE_ADMIN_GROUP_ID
   ```

2. **ตรวจสอบ Console Logs:**
   - ดู server logs หาข้อความ error
   - มองหา "LINE notification sent successfully" เพื่อยืนยันว่าส่งสำเร็จ

3. **ตรวจสอบ Channel Access Token:**
   - Token อาจจะหมดอายุ ลองออก token ใหม่
   - ตรวจสอบว่า copy token ถูกต้องไม่มีช่องว่างเกิน

4. **ตรวจสอบ User ID / Group ID:**
   - ตรวจสอบว่า User ID ถูกต้อง (เริ่มต้นด้วย `U`)
   - ตรวจสอบว่า Group ID ถูกต้อง (เริ่มต้นด้วย `C`)
   - ตรวจสอบว่า Bot ถูกเชิญเข้ากลุ่มแล้ว (สำหรับ Group)

5. **ทดสอบด้วย API โดยตรง:**
   ```bash
   curl -X POST https://api.line.me/v2/bot/message/push \
     -H 'Content-Type: application/json' \
     -H 'Authorization: Bearer YOUR_CHANNEL_ACCESS_TOKEN' \
     -d '{
       "to": "USER_OR_GROUP_ID",
       "messages": [
         {
           "type": "text",
           "text": "Test notification"
         }
       ]
     }'
   ```

### Error: "LINE notification recipient ID not configured"

- ตรวจสอบว่าได้ตั้งค่า `LINE_ADMIN_USER_ID` หรือ `LINE_ADMIN_GROUP_ID` แล้ว
- รีสตาร์ท development server หลังเพิ่ม environment variables

### Error: 401 Unauthorized

- Channel Access Token ไม่ถูกต้องหรือหมดอายุ
- ลองออก token ใหม่จาก LINE Developers Console

### Error: 400 Bad Request

- User ID หรือ Group ID ไม่ถูกต้อง
- ตรวจสอบรูปแบบของ ID (User ID ต้องขึ้นต้นด้วย `U`, Group ID ต้องขึ้นต้นด้วย `C`)

## ฟีเจอร์เพิ่มเติม

คุณสามารถเพิ่มการแจ้งเตือนแบบอื่นๆ ได้โดยใช้ฟังก์ชันที่มีอยู่ใน `src/lib/line-notify.ts`:

### แจ้งเตือนเมื่อ Seller กรอกข้อมูลเสร็จ

```typescript
import { notifySellerProfileUpdate } from '@/lib/line-notify'

await notifySellerProfileUpdate({
  email: 'user@example.com',
  fullName: 'John Doe',
  phone: '081-234-5678',
  updateType: 'profile_completed'
})
```

### ส่งข้อความกำหนดเอง

```typescript
import { sendCustomLineNotification } from '@/lib/line-notify'

await sendCustomLineNotification('🚨 มี booking ใหม่จำนวน 10 รายการ!')
```

## การ Deploy ไปยัง Production

1. เพิ่ม environment variables ใน hosting platform ของคุณ:
   - Vercel: Settings → Environment Variables
   - DigitalOcean: App Settings → Environment
   - ฯลฯ

2. ตรวจสอบว่า Channel Access Token ที่ใช้เป็น production token

3. ทดสอบ notification หลัง deploy:
   - สร้าง test account
   - สมัครสมาชิกใหม่
   - ตรวจสอบว่าได้รับ notification

## ข้อมูลเพิ่มเติม

- [LINE Messaging API Documentation](https://developers.line.biz/en/docs/messaging-api/)
- [LINE Bot SDK for Node.js](https://github.com/line/line-bot-sdk-nodejs)
- [LINE Official Account Manager](https://manager.line.biz/)
- [LINE Developers Console](https://developers.line.biz/console/)

## การสนับสนุน

หากมีปัญหาหรือข้อสงสัย:
1. ตรวจสอบ console logs เพื่อดู error messages
2. ตรวจสอบว่า environment variables ตั้งค่าถูกต้อง
3. ทดสอบ API endpoint โดยตรงด้วย curl หรือ Postman
4. ดูเอกสาร LINE Messaging API อย่างละเอียด
